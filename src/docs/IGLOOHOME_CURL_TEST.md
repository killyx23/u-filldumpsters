# Igloohome CURL Test Commands

## ⚠️ Important Note
The current system uses a **Postgres RPC function**, not a Supabase Edge Function. Therefore, traditional curl commands to Edge Functions won't work.

## Testing the RPC Function

### Method 1: Using Supabase SQL Editor (Recommended)

1. Log into Supabase Dashboard
2. Navigate to SQL Editor
3. Run the following query: